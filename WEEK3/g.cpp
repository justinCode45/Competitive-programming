#include <iostream>

using namespace std;

int main()
{
    int N, M, P;
    cin >> N >> M >> P;
    int crank[N];
    int back[M];
    for (int i = 0; i < N; i++)
        cin >> crank[i];
    for (int i = 0; i < M; i++)
        cin >> back[i];
    

    return 0;
}